═══════════════════════════════════════════════════════════════════
  🚀 KÖR JOBCRAFTAI - SÄKRA VERSIONEN (2025-11-22)
═══════════════════════════════════════════════════════════════════

✅ SÄKERHETSFIXAR SLUTFÖRDA:
- API-nycklar saniteras i logs
- Email-validering (RFC 5322)
- URL-validering (HTTP/HTTPS whitelist)
- Lösenord via environment variables

───────────────────────────────────────────────────────────────────
📋 STEG 1: SÄTT ENVIRONMENT VARIABLE (VIKTIGT!)
───────────────────────────────────────────────────────────────────

Öppna PowerShell och kör:

$env:JOBCRAFT_API_KEY = "your-openai-api-key-here"

Verifiera:
echo $env:JOBCRAFT_API_KEY

Ska visa: sk-proj-... (din API-nyckel)

───────────────────────────────────────────────────────────────────
▶️ STEG 2: KÖR PROGRAMMET
───────────────────────────────────────────────────────────────────

python main.py

───────────────────────────────────────────────────────────────────
🎯 VAD HÄNDER NU?
───────────────────────────────────────────────────────────────────

1. Programmet startar och visar en meny
2. Välj vad du vill göra:
   → Generate Resume
   → Generate Resume Tailored for Job Description
   → Generate Tailored Cover Letter for Job Description
   → Generate and Send Job Application via Email

3. Välj CV-modell:
   → URSPRUNGLIGA (klassiska mallar)
   → MODERN_DESIGN_1 (professionella moderna)
   → MODERN_DESIGN_2 (kreativa med sidopanel)

4. Välj specifik mall från vald modell

5. Ange jobb-URL (om du valde "Tailored")
   - URL valideras automatiskt! ✅
   - Endast HTTP/HTTPS tillåts
   - Farliga URLs blockeras

6. Vänta medan AI genererar (~20 sekunder)

7. PDF sparas i: data_folder/output/[hash]/

───────────────────────────────────────────────────────────────────
📧 EMAIL-FUNKTION (Valfritt)
───────────────────────────────────────────────────────────────────

Om du vill skicka email automatiskt:

1. Skaffa Gmail App Password:
   - Gå till: https://myaccount.google.com/apppasswords
   - Aktivera 2-faktor först
   - Generera lösenord för "Mail"
   - Kopiera lösenordet (16 tecken)

2. Sätt environment variable:
   $env:JOBCRAFT_SMTP_PASSWORD = "ditt-app-password"

3. Uppdatera email_config.yaml:
   - Ändra din email
   - Ändra ditt namn
   - TA BORT password-raden (hämtas från env!)

4. Kör programmet och välj:
   "Generate and Send Job Application via Email"

───────────────────────────────────────────────────────────────────
⚠️ TROUBLESHOOTING
───────────────────────────────────────────────────────────────────

Problem: "Module not found: src.security_utils"
Lösning: 
  pip install -r requirements.txt

Problem: "API key not found"
Lösning: 
  echo $env:JOBCRAFT_API_KEY
  # Om tom, sätt den igen (se STEG 1)

Problem: "Invalid email format"
Lösning: 
  - Detta är en säkerhetsfunktion!
  - Använd korrekt format: user@domain.com
  - Inga specialtecken som ; | & tillåts

Problem: "Invalid URL scheme"
Lösning:
  - Detta är en säkerhetsfunktion!
  - Använd endast http:// eller https://
  - file:// och javascript: blockeras av säkerhetsskäl

───────────────────────────────────────────────────────────────────
📚 MER INFORMATION
───────────────────────────────────────────────────────────────────

Fullständig guide:
  - QUICK_START_AFTER_SECURITY_FIX.md
  - SECURITY_SETUP_GUIDE.md
  - SÄKERHETSFIXAR_SAMMANFATTNING.md

Logs:
  - log/app.log (allmän logg)
  - log/selenium.log (browser-aktivitet)

Output:
  - data_folder/output/[hash]/resume_*.pdf
  - data_folder/output/[hash]/cover_letter_*.pdf

───────────────────────────────────────────────────────────────────
✅ SÄKER ATT KÖRA!
───────────────────────────────────────────────────────────────────

Alla kritiska säkerhetsproblem är fixade:
✅ API-nycklar loggas inte längre
✅ Emails valideras (injection-skydd)
✅ URLs valideras (XSS/SSRF-skydd)
✅ Lösenord i environment variables (säkert)

Kör nu:
  python main.py

Lycka till! 🚀

═══════════════════════════════════════════════════════════════════

